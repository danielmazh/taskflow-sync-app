Date: 2026-06-14
Phase 5e — final polish + closeout. View-layer only; no new deps; no model/persistence/notification changes.
On-time % is now onTime / (onTime + late + missed) — missing a deadline counts against you the same as completing late. hasOnTimeData = (onTime+late+missed) > 0; noData only when all three are zero.
Add sheet: a fixed `IconButton.filledTonal` (calendar icon, tooltip "Pick date & time") sits on the LEFT — primary, always visible, always one tap. To its right is a horizontally-scrollable convenience-chip row: Today / This evening / Tomorrow / Next week. Removed the trailing "Pick…" chip from the row to avoid forcing a swipe-to-end. Pure helper `quickDateOptions(now)` computes times (Today = 17:00 with "+1h rounded to :30" fallback past 17, clamped to 23:30; This evening = 18:00 rolling to tomorrow if past; Tomorrow / Next week = +1/+7 days at 09:00). Title auto-focuses on new-task open (skipped for edit). Each TextField (title + note) has its own mic suffix-icon → showVoiceCaptureSheet → recognized text appended to that field; the Home AppBar mic is preserved.
Undo: util/undo_snackbar.dart wraps ScaffoldMessenger w/ 4s SnackBar + UNDO; cancels in-flight first. Wired on Home (complete via toggle, swipe-delete via Dismissible) and Archive (per-row delete). Restore = `store.add(snapshot)`; reconcile reschedules the notification automatically if dueAt is set.
Animations: TaskCard title uses AnimatedDefaultTextStyle (220ms easeOut) for the strike/un-strike transition; Home rows fade-in via TweenAnimationBuilder(opacity 0→1). No AnimatedList — implicit anims are enough for the slice.
A11y: Semantics labels on the donut and the weekly bars (screen-reader-friendly summaries), tooltips on icon buttons + chips, M3 default ≥48dp tap targets, no color-only status (badges + icons everywhere).
Closeout: project complete. Phases 0 → 5e shipped on stack: Flutter 3.44.1 / Dart 3.12.1, shared_preferences+JSON storage, ChangeNotifier state, flutter_local_notifications scheduling, optional speech_to_text voice.
Phase 4 prep (NOT done in 5e): debug SHA-1 fingerprint for Android OAuth client = `B0:10:0F:60:A7:1A:DD:C3:8E:F7:B2:61:1E:1F:53:60:13:85:87:65` (keystore: ~/.android/debug.keystore, alias AndroidDebugKey).
Numbering note: 017 was already used for the release-APK convention from the 5d→5e handoff; this closeout takes 018. Keep the prefix sequential — do not retro-renumber.
