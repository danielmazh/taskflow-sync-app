Date: 2026-06-15
Insight: Phase 7a — labels = one `String? label` on Task, persisted inside Task JSON in shared_preferences. No new dep, no new storage. `fromJson` defaults null for legacy tasks (mirrors calendarEventId pattern). TaskStore stays pure data: adds `labels` getter (distinct, case-insensitive-deduped, first-seen casing, sorted) and a `label` param on `update()`. Filter/search are HomeScreen view state for 7b/7c — never in TaskStore, never persisted. AddTaskSheet has a label TextField + chip strip of `knownLabels` from `store.labels`. TaskCard shows a muted chip only when label non-empty. Undo snapshots (HomeScreen._onDelete, ArchiveScreen) include `label` and intentionally omit `calendarEventId` (store.delete drops the remote event). Indexed 033 to dodge collision with parallel branch feature/auth-error-surfacing's unmerged 032.
Rule: For labels never add a dep or storage; `String? label` in Task JSON is the whole shape. Filter/search = view state, not store. Every Task(...) reconstruction must include `label`.
