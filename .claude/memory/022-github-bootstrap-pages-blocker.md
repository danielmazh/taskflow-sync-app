Date: 2026-06-14
Insight: Repo bootstrap and GitHub Pages hosting are live. Origin = https://github.com/danielmazh/taskflow-sync-app (owner danielmazh, now public). master = production baseline (merge-only); pre-master = integration trunk; feature/* = one-per-change, merged --no-ff into pre-master then deleted. Initial baseline commit c5289be on master; pre-master at 683fc9c (placeholder substitution merged in).
Pages: source = pre-master branch, /docs folder. Live URLs (200) — https://danielmazh.github.io/taskflow-sync-app/ and https://danielmazh.github.io/taskflow-sync-app/privacy-policy.html. Enabled in the UI by the owner (danielmazh) because the bot collaborator cr-DanielMazhbits has only 'write', not 'admin'.
Identity: repo-local git config user.name=cr-DanielMazhbits, user.email=100853323+cr-DanielMazhbits@users.noreply.github.com (never set globally). gh auth is the cr-DanielMazhbits keyring token.
Pages-cache caveat: Pages serves a CDN-cached snapshot; after merging into pre-master, expect 30–90s before the new content is live. If a build fails, the previous good snapshot keeps serving.
Rule: All non-bootstrap work cuts a feature branch from pre-master, merges back via --no-ff, then deletes the feature branch; never commit directly to master or pre-master, and never re-attempt Pages settings from the bot — route Pages config changes (source branch/folder, custom domain) back to the owner.
