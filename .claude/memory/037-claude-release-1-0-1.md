Date: 2026-06-15
Insight: Phase 7-inclusive Claude release built at versionName 1.0.1, versionCode 3, signed by the operator upload cert (SHA-1 `6e77c3896acdeda7abc92ecfa8b78e666004a376` — NOT AndroidDebugKey). APK at `.claude/releases/taskflow-sync-1.0.1+3.apk`, 53 MB. The +3 versionCode preserves the monotonic ladder above master's +1 and above the unmerged feature/auth-error-surfacing branch's +2. Branch `feature/claude-release-1.0.1` carries only the pubspec bump (APK is gitignored per memory 017). Existing `.claude/releases/` retains two earlier APKs (phase4 1.0.0 and 1.0.0+2 auth-surfacing); memory 017 calls for one-file-at-a-time, but the +2 belongs to an unmerged branch so cleanup is the operator's call. `apksigner` and `aapt` require `JAVA_HOME`; run them only after sourcing `.tools/env.sh` (JDK17), else apksigner silently triggers the macOS Java prompt and exits.
Rule: Always bump pubspec +N before any release build (Play rejects ties). Confirm the release SHA-1 begins `6e77c389…` before declaring a build signed; an AndroidDebugKey SHA-1 means `android/key.properties` was missing or unreadable. Run `apksigner`/`aapt` only after sourcing `.tools/env.sh`.
