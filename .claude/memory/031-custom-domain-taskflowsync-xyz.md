Date: 2026-06-14
GitHub Pages custom domain switched to taskflowsync.xyz via docs/CNAME (apex A → Pages IPs + www CNAME → danielmazh.github.io already in Hostinger DNS). docs/CNAME contains exactly 'taskflowsync.xyz\n'. Pages API confirms cname=taskflowsync.xyz, https_certificate=authorization_pending, https_enforced=false. URL swap touched only docs/oauth-consent.md, docs/play-listing.md, docs/play-testing.md — the other 3 named files (index.html, privacy-policy.html, RUNBOOK.md) had no old-URL references. PUT /pages still 404s because cr-DanielMazhbits has only write perm; CNAME file is the supported alternate path and was sufficient.
Rule: For Pages custom domains, prefer committing docs/CNAME (or root CNAME) over the API — works without admin and is the canonical source. Do NOT enable Enforce HTTPS until GitHub finishes cert authorization (https_certificate transitions away from authorization_pending).
