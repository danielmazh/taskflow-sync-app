Date: 2026-06-14
Insight: Phase 6 wires the production launcher icon + store assets. Icon sources are vector at `assets/icon/app-icon-master.svg` (full-bleed: white check on #3F51B5) and `assets/icon/app-icon-foreground.svg` (transparent canvas; check scaled 1.45× around the centre so it reads at launcher size after the Android 13+ adaptive-icon mask + flutter_launcher_icons' 16% inset — visible glyph ≈60% of the masked area, well inside the safe-zone center 66%). Vector→PNG rasterizing uses a **project-local** `resvg 0.47.0` at `.tools/bin/resvg` (downloaded from linebender/resvg GitHub releases; macOS aarch64 zip). NEVER upscale a raster master. The 1024×1024 PNGs in `assets/icon/generated/` are produced from the SVGs and consumed by `flutter_launcher_icons` (added as a `dev_dependencies` entry, build-time only — approved Tier-A exception). The launcher-icons config in pubspec.yaml uses `image_path` = master, `adaptive_icon_foreground` = foreground PNG, `adaptive_icon_background` = "#3F51B5", `min_sdk_android: 24`. Generator output: every `mipmap-{m,h,xh,xxh,xxxh}dpi/ic_launcher.png` overwritten + `drawable-*/ic_launcher_foreground.png` for adaptive + `mipmap-anydpi-v26/ic_launcher.xml` declaring the adaptive pair + `values/colors.xml` carrying the background color. Manifest label changed from `taskflow_sync` (Flutter scaffold default) → `TaskFlow Sync`. Store visual assets at `docs/store-assets/icon-512.png` and `docs/store-assets/feature-graphic-1024x500.png` — both rendered from `assets/icon/feature-graphic.svg` with resvg's `--sans-serif-family "Helvetica Neue"` flag (resvg uses the macOS system font registry; on a different host the font flag must change). The release pipeline compiles cleanly: `flutter build appbundle --release` produced a 53.6 MB AAB (debug-fallback signed, since `android/key.properties` is absent — operator must create it before a Play upload per memory 029). 82 tests still green; analyze clean. On-device validation on the S25 Ultra (R5CY9256K2P): launcher shows "TaskFlow Sync" + the new check icon (legacy + adaptive crops cleanly to circle/squircle); versionCode=1 / versionName=1.0.0 confirmed via dumpsys.
Rule: `flutter_launcher_icons` is the ONLY approved package-set exception for build-time icon generation — it sits in `dev_dependencies` and ships nothing in the app. Adding ANY other dep (build-time or runtime) outside the locked Phase 2/3 list is Tier-B. NEVER raster-upscale the master icon — re-render from the SVG via `.tools/bin/resvg` at the target dimensions. When re-generating, keep the foreground glyph at ~60% of canvas so adaptive icon masking doesn't shrink it below readable size on a launcher with aggressive inset.
